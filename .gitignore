# Personal automation, OS noise
.DS_Store

# Build output folders (regenerated by `docker build` / `acap-build`)
build/

# Prebuilt ACAP packages are distributed via GitHub Releases, not tracked in git
*.eap
